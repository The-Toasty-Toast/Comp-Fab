/*For the triangle I kind of just chose a random first length and 
angle then realized I would actually need to do some trig. to 
figure out the hypotenuse. I used SOHCAHTOA to find the hypotenuse 
*/

import Turtle.*;

Turtle t; 

void setup() {
  size(500, 500);
  background(0);
  stroke(255);
  strokeWeight(2);
  t = new Turtle(this);
// isosceles triangle
    t.right(67);
    t.forward(50);
    t.right(45);
    t.forward(50);
    t.right(157.5);
    t.forward(92.39);
// ha ha yep breaking out the trig for this XD 
}
